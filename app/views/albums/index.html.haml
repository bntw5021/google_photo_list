%h1
  アルバム一覧
%table
  - @albums.each do |album|
    %tr
      %th= album.id
      %td= album.title
      %td= album.album_url
      %td= album.cover_url
      %td= album.count
      %td= link_to "削除する", album, method: :delete
= link_to "アルバムを追加する", new_album_path
= link_to "更新履歴を見る", histories_path
